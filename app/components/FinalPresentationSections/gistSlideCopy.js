// gistSlideCopy.js
// Instructional wording for the gist stage.
//
// This is neither store data (it isn't teacher-authored) nor theme data (a
// theme shouldn't decide what the task says). It lives on its own so that
// wording changes — or a future per-level / per-language variant — happen in
// one file without touching the store or any theme.

export const GIST_SLIDE_COPY = {
  title: "Listen for",
  titleAccent: "Gist",
  listenLabel: "Listen once.",
  answerLabel: "Answer:",
  // Shown only if the teacher never selected a gist question.
  questionFallback: "What is the main topic?",
};
